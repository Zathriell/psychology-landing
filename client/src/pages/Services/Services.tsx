import { Typography, Container } from "@mui/material";

const Services = () => {
  return (
    <Container>
      <Typography variant="h3">
        Servicios
      </Typography>
    </Container>
  );
};

export default Services;